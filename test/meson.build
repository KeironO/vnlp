core_test = executable(
    meson.project_name() + '-test',
    'TestTokeniser.vala',
    meson.source_root() + '/src/Tokeniser.vala',
    dependencies: dependencies
)

test('Tokeniser Test', core_test)
